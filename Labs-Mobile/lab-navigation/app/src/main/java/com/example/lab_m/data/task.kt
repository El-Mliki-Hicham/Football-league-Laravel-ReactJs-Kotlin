package com.example.lab_m.data
//data class it uses for represent object that are immutable and automatically generate methods: equale(),hashCode,and toString();

data class task(val id:Int, var Task :String, val Priority:Int, val TimeTemps:Long)